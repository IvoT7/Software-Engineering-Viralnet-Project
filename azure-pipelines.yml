trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
# This command prints every file and folder in your project to the log
- script: |
    echo "--- Listing everything in the repository ---"
    find . -maxdepth 3 -not -path '*/.*'
  displayName: 'DEBUG: Find my folders'