trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: |
    echo "--- CURRENT DIRECTORY ---"
    pwd
    echo "--- FILE STRUCTURE ---"
    find . -maxdepth 4 -not -path '*/.*'
  displayName: 'Debug File Structure'